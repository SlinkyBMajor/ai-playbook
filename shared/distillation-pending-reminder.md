Files in this project have been edited since the last `/playbook:distil` run.

If the developer's current message indicates work is wrapping up — completion, ready to commit, "done", "ship it", or similar — surface `/playbook:distil` as the next step so any durable knowledge can be captured.

Otherwise ignore this reminder. Distillation is developer-gated; do not invoke it autonomously.
